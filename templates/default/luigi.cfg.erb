# Configuration file generated by Chef
# Default values must be defined through luigi cookbook
<% (@luigi_cfg || {}).each do |section, values| %>
[<%= section %>]
<% (values || {}).each do |property, value| %>
<%= property %> = <%= value %>
<% end -%>

<% end -%>
